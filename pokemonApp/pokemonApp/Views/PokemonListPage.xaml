<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="pokemonApp.Views.PokemonListPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <ContentPage.Content>


        <CollectionView ItemsSource="{Binding pokeList}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="15,20,5,5" Orientation="Horizontal">
                        <Label Text="{Binding Id}" TextColor="Black" />
                        <Label Text="{Binding Name}" TextColor="Black" />
                        <Label Text="{Binding Weight}" TextColor="Black" />
                        <Image Source="{Binding Picture}" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentPage.Content>
</ContentPage>
